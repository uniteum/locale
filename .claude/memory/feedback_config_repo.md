---
name: config-repo-role
description: The uniteum config repo is called "config", not "locale" — locale is a different project
type: feedback
---

Do not refer to `locale` as the place to store common/shared configuration. The shared config repo is `config`.

**Why:** User corrected this explicitly — locale is a separate project, not the config repo.
**How to apply:** When suggesting where to store shared files across uniteum projects, reference `config`.
